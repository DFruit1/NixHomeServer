{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the YouTube Downloader main window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
